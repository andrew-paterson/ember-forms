import generateEmberValidatingFormField from 'ember-starter/utils/generate-ember-validating-form-field';
import { module, test } from 'qunit';

module('Unit | Utility | generate-ember-validating-form-field', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generateEmberValidatingFormField();
    assert.ok(result);
  });
});
