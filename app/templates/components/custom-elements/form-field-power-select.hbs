{{#power-select
  options=formField.options
  selected=(readonly formField.value)
  searchPlaceholder=formField.searchPlaceholder
  allowClear=true
  placeholder=formField.label
  onchange=(action onSelectClick)
  onclose=onclose
  as |option|
  }}
  {{option}}
{{/power-select}}
